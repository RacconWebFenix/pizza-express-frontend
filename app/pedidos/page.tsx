"use client";

import { PedidosContainer } from "../../components/pedidos";

/**
 * Página principal de pedidos
 * 
 * Esta página é responsável apenas por renderizar o container de pedidos,
 * seguindo o princípio de responsabilidade única.
 */
export default function PedidosPage() {
  return <PedidosContainer />;
}
